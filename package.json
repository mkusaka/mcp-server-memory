{
  "name": "@mkusaka/mcp-server-memory",
  "version": "0.0.2",
  "description": "MCP server for enabling memory for Claude through a knowledge graph",
  "license": "MIT",
  "author": "Anthropic, PBC (https://anthropic.com), mkusaka (https://github.com/mkusaka)",
  "homepage": "https://github.com/mkusaka/mcp-server-memory",
  "bugs": "https://github.com/mkusaka/mcp-server-memory/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/mkusaka/mcp-server-memory.git"
  },
  "type": "module",
  "bin": {
    "mcp-server-memory": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "pnpm run build",
    "watch": "tsc --watch"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2"
  }
}
